/** * Created by 1ka on 4/9/14. */ZOOMPYCDB.namespace("ZOOMPYCDB.Logger");ZOOMPYCDB.Logger = function (model, elements) {    var error_message = elements.message,        printErrorMessage = function (message) {            error_message.text("Error message: " + message).fadeIn(1000);            setTimeout(function () {                error_message.fadeOut(2000);            }, 3000);        },    //cursor functions        setLoadingCursor = function () {            document.body.style.cursor = 'wait';        },        unsetLoadingCursor = function () {            document.body.style.cursor = 'default';        };    $(document).on("error_message", function (event, mes) {        printErrorMessage(mes);    });    $(document).on("set_loading_cursor", function () {        setLoadingCursor();    });    $(document).on("unset_loading_cursor", function () {        unsetLoadingCursor();    });    return{        setLoadingCursor: setLoadingCursor,        unsetLoadingCursor: unsetLoadingCursor    };};