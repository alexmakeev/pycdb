/** * Created by 1ka on 4/5/14. */ZOOMPYCDB.namespace("ZOOMPYCDB.ScemeController");ZOOMPYCDB.ScemeController = function(models,view){    var storage = models.storage,        temp_data = models.temp,        graph_view = view.graph;    $(document).on("cid_chosen",function(event,cid){        temp_data.setChosenCid(cid);    });    $(document).on("rid_chosen",function(event,rel){        temp_data.setChosenRel(rel);        storage.highlightRelSources(rel);    });    $(document).on("add_node",function(){        var coord = graph_view.getMouseCoord();        if(coord!==undefined)            storage.addNode(temp_data.getChosenCid(),coord)    });}